# See https://nextjs.org/docs/app/api-reference/config/typescript for more about
# these files. Mirrors web/.gitignore for the same reasons.

# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# env files — real values stay local, the template is committed
.env*
!.env.example

# typescript
*.tsbuildinfo
